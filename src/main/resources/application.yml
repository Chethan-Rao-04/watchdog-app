spring:
  application:
    name: watchdog
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:addtheusername}
    password: ${MAIL_PASSWORD:addthepwd} # Use env vars in production
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

watchdog:
  check-interval: ${CHECK_INTERVAL:30000}
  summary-interval: ${SUMMARY_INTERVAL:0 14 19 * * SUN} # Default Sunday 7:14 PM
  email-recipients: ${EMAIL_RECIPIENTS:chethanrao82@gmail.com}
  mail-username: ${MAIL_USERNAME:chethancn82@gmail.com}
  mail-password: ${MAIL_PASSWORD:ENC(fdaiqnjqyeizulvy)}

jasypt:
  encryptor:
    password: ${JASYPT_ENCRYPTOR_PASSWORD:your-secret-key}

logging:
  level:
    com.charite.watchdog: DEBUG  # Show DEBUG, INFO, WARN, ERROR from your package
